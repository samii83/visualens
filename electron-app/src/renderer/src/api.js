// electron-app/src/renderer/src/api.js
// Purpose: Wrapper for renderer to call backend FastAPI endpoints.
// TODO: Implement real fetch/axios calls.

// Exports (example):
// - getCaption(fileOrBuffer)
// - getEmbedding(fileOrBuffer or text)
// - search(queryObj)
// - listImages()

// Notes:
// - In development: use `http://localhost:8000` or configure via env var
// - In production: use the `preload` bridge or a local URL provided by the main process
